let fs = require("fs");
const path = require("path");

const editCreationControler = {
    creationproduct: function(req, res) {
       return res.render('editionProducts')
    }
}
module.exports = editCreationControler; 