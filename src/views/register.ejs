<!DOCTYPE html>
<html lang="es">

<!--HEAD-->
<%- include('./partials/head') %>

 
<body>
    <!--mostrador de errores-->
    <% if(typeof errors != 'undefined') { %>
        <p style="top: 3em; color: red;">ERROR</p>
        
        <ul>
            <% for (let i = 0; i < errors.length; i++) { %>
                <li>
                    <%= errors[i].msg%>
                </li>
            <% } %>
        </ul>
    <% } %>



<div class="container">
    <h2>Crea tu cuenta!</h2>

    <p>Regístrate para acceder a tus cursos.</p>

    <form method="POST" action="/user/register" >
        <label for="nombre">Nombre:</label>
        <input type="text" id="nombre" name="name" placeholder="name" required>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" placeholder="Email" required>
        <% if (locals.errors && errors.email) { %>
            <div class="text-denger">
                <%= errors.email.msg %>
            </div>
        <% } %>
        <label for="contraseña">Contraseña</label>
        <input type="password" id="contraseña" name="password" placeholder="contraseña">
        <input type="submit" value="Crea tu cuenta">
    </form>
</div>
</body>
</html>